"use client";

import VisiOptScript from "./VisiOptScript";

/**
 * Component for loading VisiOpt scripts on the order received (thank you) page
 */
export default function ThankYouScript() {
  return <VisiOptScript pid={15} />;
}
